# Configuración del Servidor
PORT=8000
NODE_ENV=development

# Seguridad - Sesiones
# IMPORTANTE: Genera un secret aleatorio para producción
# Puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-me-in-production

# Base de Datos
# Si DATABASE_URL está presente, usará PostgreSQL (Railway lo provee automáticamente)
# Si no está presente, usará SQLite (modo desarrollo)
# DATABASE_URL=postgresql://user:password@host:5432/database
